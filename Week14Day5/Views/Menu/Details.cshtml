@model MenuViewModel
<h1>MENU</h1>

<table class="table table-hover table-bordered table table-striped">
    <tbody>
        <tr>
            <th>ID</th>
            <td>@Model.Id</td>
        </tr>
        <tr>
            <th>Nome</th>
            <td>@Model.Nome</td>
        </tr>
        <tr>
            <th>Piatti</th>
            @foreach (var item in Model.Piatti)
            {

                        <th>@item.Nome</th>
                        <td>Prezzo: @item.Prezzo Euro</td>
            }
        </tr>


    </tbody>
</table>